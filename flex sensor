#include<math.h>
void setup() {
  pinMode(32, INPUT);
  Serial.begin(115200);

}

void loop() {
  int x = 0; 
  float y = 0.0;
  float sum = 0.0;
  float A = 3.1415*(12.0/1000.0)*(12.0/1000.0);
    for(int i = 1; i<=10;i++){
      int x = analogRead(32);
      y = 6.0E-09 * x * x * x * x - 1.0E-05 * x * x * x + 0.0057 * x * x - 1.062 * x;
        if (y <= 0){ y = 0; sum = sum + y; }
        else{sum = sum+ y;}
        Serial.print(y);Serial.print("|");
    delay(10);
    }

    y = sum/10;
    
   Serial.print(" === Avg: ");Serial.print(y);Serial.println (" degree");delay(300);
   //Serial.print("----");

   
   // x = analogRead(32);
   //Serial.println(x);delay(300);
    
  
